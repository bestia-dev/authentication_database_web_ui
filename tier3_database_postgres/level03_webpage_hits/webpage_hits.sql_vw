-- tier3_database_postgres/level03_webpage_hits/webpage_hits.sql_vw

select a_migrate_view('webpage_hits',
$source_code$

create view webpage_hits
as

select w.id,
w.webpage,
h.count as hit_count
from webpage w
join hit_counter h on h.webpage_id = w.id
where w.id = h.webpage_id
order by w.webpage;

$source_code$);
