-- a_random_between.sql

-- select a_drop_function_any_param('a_random_between');
-- psql -U admin -h localhost -p 5432 -d webpage_hit_counter -f tier3_database_postgres/system_wide_objects/a_random_between.sql

select a_migrate_function('a_random_between',
$source_code$

create function a_random_between(_low integer, _high integer)
returns integer
strict as 
$$
begin

return floor(random()* (_high-_low + 1) + _low);

end;
$$ LANGUAGE plpgsql;

$source_code$);

